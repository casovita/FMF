name: fmf_data
publish_to: none
resolution: workspace

environment:
  sdk: ^3.8.0
  flutter: ">=3.41.0"

dependencies:
  flutter:
    sdk: flutter
  drift: ^2.31.0
  drift_flutter: ^0.2.8
  path_provider: ^2.1.5
  fmf_domain:
    path: ../domain

dev_dependencies:
  drift_dev: ^2.31.0
  build_runner: ^2.13.1
  flutter_lints: ^6.0.0
